@model IEnumerable<smileRed.Domain.Data>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.VirtualCounter)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.VirtualCounter)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DataID }) |
            @Html.ActionLink("Details", "Details", new { id=item.DataID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DataID })
        </td>
    </tr>
}

</table>
