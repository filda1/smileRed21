<?xml version="1.0" encoding="utf-8" ?>
<pages:PopupPage
                 xmlns:pages="clr-namespace:Rg.Plugins.Popup.Pages;assembly=Rg.Plugins.Popup"
                 xmlns="http://xamarin.com/schemas/2014/forms"
                 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                 x:Class="smileRed21.Views.ReverseGeocodeNew">

    <StackLayout VerticalOptions="Center"  Padding="20,0" HorizontalOptions="FillAndExpand" >
        <Frame CornerRadius="10" Padding="0" BackgroundColor="CadetBlue" >
            <StackLayout Padding="10">
                <Label  x:Name="Caption" TextColor="Black" FontSize="20" HorizontalOptions="Center"></Label>
                <ScrollView>
                    <StackLayout>
                        <StackLayout Orientation="Horizontal">
                            <Label x:Name="Place" TextColor="LightBlue"/>
                        </StackLayout>
                        <StackLayout Orientation="Horizontal" Margin="140,5,0,0">
                            <Button x:Name="Accept" Text="✓ Continue Order" TextColor="Orange" Clicked="Button_Open"></Button>
                            <Button x:Name="Close"  Text="X Close Order" TextColor="Red" Clicked="Button_Clicked"></Button>
                        </StackLayout>

                    </StackLayout>
                </ScrollView>
            </StackLayout>
        </Frame>
    </StackLayout>
    </pages:PopupPage>